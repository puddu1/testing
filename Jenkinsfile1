pipeline {
    agent any
    stages{
        stage('hello'){
          steps{
            sh ''' echo "hello" 
            git branch
            last_comm = git for-each-ref --sort=-committerdate refs/heads/ --format='%(committerdate:short)'
            echo " $last_comm "
            '''
          }

        }
    
    }
}
