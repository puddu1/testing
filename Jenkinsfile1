pipeline {
    agent any
    stages{
        stage('hello'){
          steps{
            sh ''' echo "hello" 
            git branch
            echo ${env.BRANCH_NAME}
            '''
          }

        }
    
    }
}
